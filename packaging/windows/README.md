# Windows Packaging

Target format: MSI.

Expected bundled tools:

- FFmpeg executable set
- ImageMagick CLI tools
- LibreOffice headless runtime
- Pandoc executable
- Qt runtime DLLs and platform plugins

Engine binaries should be placed under the install directory in `engines/`.
