# Linux Packaging

Target format: AppImage.

Expected bundled tools:

- FFmpeg executable set
- ImageMagick CLI tools
- LibreOffice headless runtime
- Pandoc executable
- Qt libraries and platform plugins required by the build

Engine binaries should be placed under the AppDir in `usr/bin/engines/` or an equivalent resolved runtime path.
